services:
  yaraxserver4:
    container_name: yaraxserver4
    image: yaraxserver:latest
    network_mode: "host"
    restart: "always"
    working_dir: /app
    command: ["/app/yaraxserver", "--port", "7779"]
    environment:
      DATABASE_URL: "postgres://postgres:Welcome1admin2024@localhost:5432/postgres"
